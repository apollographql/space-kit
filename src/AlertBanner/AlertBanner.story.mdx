import { AlertBanner } from "../AlertBanner";
import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";

<Meta title="Components|AlertBanner" />

# AlertBanner

**AlertBanners** are primarily for situations where there is nothing you can do in the UI to immediately fix or change the state (e.g. deprecation warnings, account status alerts, etc).

## Info Alert

<Preview>
  <Story name="info light">
    <AlertBanner type="info">
      This is an example of an inline info message. It should be rather short,
      but can have multi-lines if needed.
    </AlertBanner>
  </Story>
  <Story name="info dark">
    <AlertBanner type="info" theme="dark">
      This is an example of an inline info message. It should be rather short,
      but can have multi-lines if needed.
    </AlertBanner>
  </Story>
</Preview>

## Warning Alert

<Preview>
  <Story name="warning light">
    <AlertBanner type="warn">
      This is an example of an inline warning message. It should be rather
      short, but can have multi-lines if needed.
    </AlertBanner>
  </Story>
  <Story name="warning dark">
    <AlertBanner type="warn" theme="dark">
      This is an example of an inline warning message. It should be rather
      short, but can have multi-lines if needed.
    </AlertBanner>
  </Story>
</Preview>

## Error Alert

<Preview>
  <Story name="error light">
    <AlertBanner type="error" style={{ marginBottom: 20 }}>
      This is an example of an inline error message. It should be rather short,
      but can have multi-lines if needed.
    </AlertBanner>
  </Story>
  <Story name="error dark">
    <AlertBanner type="error" theme="dark" style={{ marginBottom: 20 }}>
      This is an example of an inline error message. It should be rather short,
      but can have multi-lines if needed.
    </AlertBanner>
  </Story>
</Preview>

## Success Alert

<Preview>
  <Story name="success light">
    <AlertBanner type="success">
      This is an example of an inline error message. It should be rather short,
      but can have multi-lines if needed.
    </AlertBanner>
  </Story>
  <Story name="success dark">
    <AlertBanner type="success" theme="dark">
      This is an example of an inline error message. It should be rather short,
      but can have multi-lines if needed.
    </AlertBanner>
  </Story>
</Preview>

## Props

### `Alert`

<Props of={AlertBanner} />
